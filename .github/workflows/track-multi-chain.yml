name: Track Multi Chain

on:
  workflow_dispatch:
    inputs:
      ethereum_query_id:
        description: Optional Dune query ID for Ethereum
        required: false
        type: string
      scroll_query_id:
        description: Optional Dune query ID for Scroll
        required: false
        type: string
      ton_query_id:
        description: Optional Dune query ID for TON
        required: false
        type: string
      airtable_table:
        description: Airtable table name
        required: false
        default: Table 1
        type: string
      debug_mode:
        description: Enable verbose validation output
        required: false
        default: true
        type: boolean
  schedule:
    - cron: '25 * * * *'

jobs:
  multi-chain:
    runs-on: ubuntu-latest
    env:
      DUNE_API_KEY: ${{ secrets.DUNE_API_KEY }}
      ETHEREUM_QUERY_ID: ${{ inputs.ethereum_query_id || secrets.DUNE_ETHEREUM_QUERY_ID || secrets.ETHEREUM_QUERY_ID }}
      SCROLL_QUERY_ID: ${{ inputs.scroll_query_id || secrets.DUNE_SCROLL_QUERY_ID || secrets.SCROLL_QUERY_ID }}
      TON_QUERY_ID: ${{ inputs.ton_query_id || secrets.DUNE_TON_QUERY_ID || secrets.TON_QUERY_ID }}
      AIRTABLE_API_KEY: ${{ secrets.AIRTABLE_API_KEY || secrets.AIRTABLE_TOKEN }}
      AIRTABLE_BASE_ID: ${{ secrets.AIRTABLE_BASE_ID || 'appUF24vYBBQnpeQl' }}
      AIRTABLE_TABLE: ${{ inputs.airtable_table || secrets.AIRTABLE_TABLE_NAME || 'Table 1' }}
      DEBUG_MODE: ${{ inputs.debug_mode || 'true' }}

    steps:
      - uses: actions/checkout@v4

      - name: Validate configuration
        shell: bash
        run: |
          set -euo pipefail

          missing=()
          [ -n "${AIRTABLE_API_KEY:-}" ] || missing+=("AIRTABLE_API_KEY or AIRTABLE_TOKEN")
          [ -n "${AIRTABLE_BASE_ID:-}" ] || missing+=("AIRTABLE_BASE_ID")

          chain_ids_present=0
          [ -n "${ETHEREUM_QUERY_ID:-}" ] && chain_ids_present=1
          [ -n "${SCROLL_QUERY_ID:-}" ] && chain_ids_present=1
          [ -n "${TON_QUERY_ID:-}" ] && chain_ids_present=1

          if [ -z "${DUNE_API_KEY:-}" ] || [ "$chain_ids_present" -ne 1 ]; then
            echo "SKIP_DUNE=true" >> "$GITHUB_ENV"
            dune_mode="skipped"
          else
            echo "SKIP_DUNE=false" >> "$GITHUB_ENV"
            dune_mode="enabled"
          fi

          if [ "${DEBUG_MODE:-true}" = "true" ]; then
            echo "Workflow debug summary"
            echo "- Airtable auth set: $([ -n "${AIRTABLE_API_KEY:-}" ] && echo yes || echo no)"
            echo "- Airtable base set: $([ -n "${AIRTABLE_BASE_ID:-}" ] && echo yes || echo no)"
            echo "- Airtable table: ${AIRTABLE_TABLE:-Table 1}"
            echo "- Dune API key set: $([ -n "${DUNE_API_KEY:-}" ] && echo yes || echo no)"
            echo "- Dune query ID present: $([ "$chain_ids_present" -eq 1 ] && echo yes || echo no)"
            echo "- Dune mode: ${dune_mode}"
            echo "- Ethereum query ID set: $([ -n "${ETHEREUM_QUERY_ID:-}" ] && echo yes || echo no)"
            echo "- Scroll query ID set: $([ -n "${SCROLL_QUERY_ID:-}" ] && echo yes || echo no)"
            echo "- TON query ID set: $([ -n "${TON_QUERY_ID:-}" ] && echo yes || echo no)"
          fi

          if [ ${#missing[@]} -gt 0 ]; then
            echo "::error title=Missing required Airtable configuration::Missing required values: ${missing[*]}"
            echo "Add AIRTABLE_API_KEY or AIRTABLE_TOKEN in Settings > Secrets and variables > Actions."
            echo "Dune settings are optional; without them this workflow writes a skipped analytics summary."
            exit 1
          fi

      - name: Fetch chain results
        if: env.SKIP_DUNE != 'true'
        run: |
          python3 <<'PY'
          import json, os, urllib.request

          api_key = os.environ['DUNE_API_KEY']
          mapping = {
              'ethereum': os.environ.get('ETHEREUM_QUERY_ID', '').strip(),
              'scroll': os.environ.get('SCROLL_QUERY_ID', '').strip(),
              'ton': os.environ.get('TON_QUERY_ID', '').strip(),
          }
          out = {}
          for chain, qid in mapping.items():
              if not qid:
                  continue
              req = urllib.request.Request(
                  f'https://api.dune.com/api/v1/query/{qid}/results?limit=10',
                  headers={'X-Dune-Api-Key': api_key}
              )
              with urllib.request.urlopen(req) as resp:
                  out[chain] = json.load(resp)
          with open('multi-chain-results.json', 'w', encoding='utf-8') as f:
              json.dump(out, f, ensure_ascii=False, indent=2)
          print(json.dumps({k: len(v.get('result', {}).get('rows', [])) for k, v in out.items()}))
          PY

      - name: Create skipped Dune result
        if: env.SKIP_DUNE == 'true'
        run: |
          cat > multi-chain-results.json <<'JSON'
          {
            "_status": {
              "mode": "skipped",
              "reason": "DUNE_API_KEY and at least one Dune query ID are required for live chain results. Airtable reporting remains enabled."
            }
          }
          JSON
          echo "::warning::Dune analytics skipped; Airtable summary will still be posted."

      - name: Post consolidated summary to Airtable
        run: |
          python3 <<'PY'
          import json, os, urllib.parse, urllib.request

          with open('multi-chain-results.json', 'r', encoding='utf-8') as f:
              data = json.load(f)

          if data.get('_status', {}).get('mode') == 'skipped':
              notes = data['_status']['reason']
              status = 'SKIPPED_DUNE'
          else:
              lines = []
              for chain, payload in data.items():
                  rows = payload.get('result', {}).get('rows', [])
                  preview = json.dumps(rows[:2], ensure_ascii=False)
                  lines.append(f'{chain}: {len(rows)} rows | sample={preview[:250]}')
              notes = ' || '.join(lines)[:900] if lines else 'No chain results available'
              status = 'ACTIVE'

          airtable_payload = {
              'records': [
                  {
                      'fields': {
                          'Name': 'Multi Chain Tracker',
                          'Notes': notes[:900],
                          'Status': status
                      }
                  }
              ]
          }

          table = urllib.parse.quote(os.environ.get('AIRTABLE_TABLE', 'Table 1'), safe='')
          url = f'https://api.airtable.com/v0/{os.environ["AIRTABLE_BASE_ID"]}/{table}'
          req = urllib.request.Request(url, data=json.dumps(airtable_payload).encode('utf-8'), method='POST')
          req.add_header('Authorization', f'Bearer {os.environ["AIRTABLE_API_KEY"]}')
          req.add_header('Content-Type', 'application/json')
          with urllib.request.urlopen(req) as resp:
              print(resp.read().decode('utf-8'))
          PY
