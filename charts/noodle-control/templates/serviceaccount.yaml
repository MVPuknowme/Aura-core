{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "noodle-control.serviceAccountName" . }}
  labels:
    {{- include "noodle-control.labels" . | nindent 4 }}
{{- end }}
