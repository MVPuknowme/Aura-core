apiVersion: v1
kind: Service
metadata:
  name: {{ include "noodle-control.fullname" . }}
  labels:
    {{- include "noodle-control.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "noodle-control.selectorLabels" . | nindent 4 }}
