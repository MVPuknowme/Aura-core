{
  "version": 2,
  "installCommand": "npm install --ignore-scripts",
  "buildCommand": "echo 'SkyGrid static landing ready'",
  "routes": [
    {
      "src": "/api/health",
      "dest": "/api/health.js"
    },
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
